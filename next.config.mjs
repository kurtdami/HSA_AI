/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      // Add your image domains here
      'your-domain.com',
      'another-domain.com'
    ],
  },
};

export default nextConfig;
